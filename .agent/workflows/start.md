---
description: Pick up the next uncompleted task from your backlog and initiate implementation.
---

1. Read your specific backlog file (e.g., `directives/01_backend_backlog.md`).
2. Identify the first task marked as `[ ]`.
3. Change its status to `[/]` (in-progress) in the file.
4. Read the target files related to this task (e.g., `main.mo` or a React component).
5. Outline a step-by-step implementation plan for THIS specific task in your next response.
6. Ask the user for confirmation before writing any code.
7. Update the `directives/00_master_plan.md` status if the task initiates a significant sub-phase.
